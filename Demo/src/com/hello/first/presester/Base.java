package com.hello.first.presester;

public class Base {

//	protected String onErrorMsg(Mode mode, int status) {
//		if (mode == Mode.LOADMORE && status == ApiModle.STATUS_EMPTY) {
//			return "暂时没有更多";
//		} else if (status == ApiModle.STATUS_ERROR) {
//			return "服务器操作异常";
//		} else if (status > ApiModle.STATUS_EMPTY) {
//			if (status == ERROR_NET_TIMEOUT) {
//				return "连接服务器超时，请稍后再试";
//			} else if (status == CLIENT_NET_DISCONNECTED) {
//				return "与服务器断开连接";
//			} else if (status == NET_NO_CONNECTION) {
//				return "暂时无法连接到服务器，请稍后再试";
//			} else if (status == ERROR_SERVER) {
//				return "操作异常";
//			}
//		}
//		return null;
//	}
}
